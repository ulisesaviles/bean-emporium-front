// Local styles
import styles from "./styles.module.css";

// Metadata
export const metadata = {
  title: "Bean emporium | Dashboard",
};

// Main React component
const Login = () => {
  return (
    <main className={styles.main}>
      <p>Login</p>
    </main>
  );
};

export default Login;
