// Local styles
import styles from "./styles.module.css";

// Metadata
export const metadata = {
  title: "Bean emporium | Orders",
};

// Main React component
const Orders = () => {
  return (
    <main className={styles.main}>
      <p>Orders</p>
    </main>
  );
};

export default Orders;
