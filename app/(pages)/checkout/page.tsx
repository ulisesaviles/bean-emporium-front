// Local styles
import styles from "./styles.module.css";

// Metadata
export const metadata = {
  title: "Bean emporium | Checkout",
};

// Main React component
const Checkout = () => {
  return (
    <main className={styles.main}>
      <p>Checkout</p>
    </main>
  );
};

export default Checkout;
